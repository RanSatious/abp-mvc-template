@{
    ViewBag.ActiveMenu = PageNames.Administration.Roles;
    ViewBag.JsPath = "app/admin/roles";
}
@section styles {
    <link rel="stylesheet" href="~/Assets/node_modules/jstree/dist/themes/default/style.min.css" />
}
<form class="layui-form layui-form-pane">
  <div class="layui-form-item">
    <div class="layui-inline">
      <label class="layui-form-label">角色名</label>
      <div class="layui-input-inline">
        <input type="text" name="roleName" autocomplete="off" class="layui-input">
      </div>
    </div>
    <div class="layui-inline">
      <div class="layui-input-inline">
        <button class="layui-btn" lay-submit lay-filter="search">查询</button>
        <button class="layui-btn layui-btn-normal" id="btn-add" lay-filter="">新增</button>
      </div>
    </div>
  </div>
</form>
<table id="main-table" lay-filter="role"></table>
<script type="text/html" id="actionTpl">
  <button class="layui-btn layui-btn-normal layui-btn-xs" lay-event="edit">编辑</button>
  <button class="layui-btn {{ d.isStatic ? 'layui-btn-disabled' : 'layui-btn-danger' }} layui-btn-xs" {{ d.isStatic ? 'disabled' : '' }} lay-event="delete">删除</button>
</script>